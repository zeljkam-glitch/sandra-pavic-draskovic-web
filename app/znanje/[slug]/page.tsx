import {notFound} from "next/navigation";import {ContentPage} from "../../content-page";import {articles} from "../data";
export function generateStaticParams(){return articles.map(a=>({slug:a.slug}));}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=articles.find(x=>x.slug===slug);if(!a)notFound();return <ContentPage eyebrow="Blog" title={a.title} intro={a.intro}><p className="fine">Urednički nacrt. Sandrina stručna provjera još nije provedena.</p>{a.sections.map(([title,text])=><section key={title}><h2>{title}</h2><p>{text}</p></section>)}<h2>Izvori</h2><ul>{a.sources.map(([label,url])=><li key={url}><a href={url} target="_blank" rel="noreferrer">{label} ↗</a></li>)}</ul><p className="fine">Opće informacije ne zamjenjuju individualni liječnički savjet.</p><a className="arrow-link" href="/znanje">Svi članci →</a></ContentPage>}
