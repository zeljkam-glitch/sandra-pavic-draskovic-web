import {ContentPage} from "../content-page";import {articles} from "./data";
export default function Page(){return <ContentPage eyebrow="Blog" title="Pitanja iz svakodnevnog života." intro="Prehrana, dodaci i priprema za savjetovanje. Kratki tekstovi s izvorima, bez univerzalnih protokola."><p className="fine">Urednički nacrti za Sandrinu stručnu provjeru prije javne objave.</p>{articles.map(a=><section key={a.slug}><h2><a href={"/znanje/"+a.slug}>{a.title}</a></h2><p>{a.intro}</p><a className="arrow-link" href={"/znanje/"+a.slug}>Pročitaj tekst →</a></section>)}</ContentPage>}
