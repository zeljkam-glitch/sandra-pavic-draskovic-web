import {ContentPage} from "../content-page";import {articles,externalArticles} from "./data";
export default function Page(){return <ContentPage eyebrow="Blog i objave" title="Sandrini tekstovi, razgovori i korisna objašnjenja." intro="Na jednom mjestu okupljamo provjerene objave o Sandri te nove edukativne tekstove o prehrani, dodacima i pripremi za savjetovanje."><h2>Objavljeno o Sandri</h2><p>Izvorne objave otvaraju se na stranicama njihovih izdavača.</p><div className="external-articles">{externalArticles.map(a=><a key={a.href} href={a.href} target="_blank" rel="noreferrer"><span>{a.outlet}</span><strong>{a.title}</strong><p>{a.description}</p><b>Pročitaj izvornu objavu ↗</b></a>)}</div><h2>Novi edukativni tekstovi</h2><p className="fine">Ovi tekstovi su urednički nacrti i čekaju Sandrinu stručnu provjeru prije javne objave.</p>{articles.map(a=><section key={a.slug}><h2><a href={"/znanje/"+a.slug}>{a.title}</a></h2><p>{a.intro}</p><a className="arrow-link" href={"/znanje/"+a.slug}>Pročitaj tekst →</a></section>)}</ContentPage>}
